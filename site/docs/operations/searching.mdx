---
title: "Searching: needles, prefixes, slices"
sidebar_label: searching
description: "The short-circuit engine and its loop-shape lesson, the backward scans, and the slice-search family that beats the textbook algorithm by not being one — every chart Int beside String."
---

import Link from "@docusaurus/Link";
import BenchPair from "@site/src/components/BenchPair";
import Related from "@site/src/components/Related";

<p className="lede">
  Everything on this page answers a question and stops: is this element here, where does this run
  end, does this sequence contain that one. Half the family is about <em>stopping well</em> — the
  short-circuit engine — and the other half is about <em>looking less</em>: the slice-search ops win
  2–3× not by a cleverer algorithm but by refusing to examine elements the answer doesn't need.
  Every chart runs Int beside String, so you can see which effects are boxing and which are
  structure.
</p>

## The short-circuit engine

`find`, `exists`, `forall`, `indexWhere`, `indexOf`, `contains` share one engine: a forward scan
that must be able to stop at any element. It carries this library's founding loop-shape lesson.
The first implementations ran at 0.2–0.4× of a raw array, and every intuitive suspect — virtual
dispatch, the tree, the consumer object — was individually ruled out by measurement. The real cost
was the *shape* of the loop body: writing result/stop flags mid-loop kept the JIT from treating the
scan as a simple counted loop. The fix was structural — an empty-bodied loop with the predicate in
the loop *condition* — and it took the family from a 3–5× deficit to the raw array's doorstep:

<BenchPair
  int="ShortCircuitIntBenchmark"
  str="ShortCircuitStrBenchmark"
  caption="exists / find / indexWhere with the match near the start — the case where stopping matters, measured fully serialized. The verdict strip is honest: FArray runs at 0.87–0.99× of the RAW ARRAY (a stopped-at-element-5 scan is a nano-op; the bare array's loop with no type test is the floor) and ahead of the wrapped field — List and Vector pay per-element machinery even for a five-element scan; on String, find actually edges List at every size past 1k. The gap to IArray is the same wrapper tax the indexed-access page prices at a lone read; it does not compound: a real scan pays it once, not per element."
/>

`contains` is the engine with equality instead of a predicate. On `Int` the compare is unboxed on
both sides — the boxing collections unwrap an `Integer` per element to answer. On references it's
an `equals` walk for everyone, and the field runs close (FArray ~1.05× over the best rival on a
full 100k miss-scan):

<BenchPair
  int="ContainsIntBenchmark"
  str="ContainsStrBenchmark"
  caption="contains on a missing element — the worst case, a full scan, nothing to short-circuit. Int beside String separates the two effects: unboxed compares versus loop tightness alone."
/>

## Backward scans

`lastIndexOf`, `lastIndexWhere` run the same engine mirrored — the traversal drivers are
forward/backward twins that flip at each `ReverseNode`, so a backward search over a reversed tree
is a *forward* scan over its child, never a materialization. The serialized numbers deserve honest
reporting: at 100k `lastIndexOf` leads ~2.1×, but through the small and mid sizes the family sits
at parity-to-slightly-behind the raw array (0.78–1.15×) — the same nano-op floor as above, paid on
a scan that finds its answer near the end it starts from. The structural win (reversed trees for
free) doesn't show in a flat-leaf benchmark at all; it shows in code that never has to call
`.reverse` first.

## Slice search: winning by not being clever

`indexOfSlice`, `lastIndexOfSlice`, `containsSlice`, `startsWith`, `endsWith` — needle-in-haystack
ops. The textbook answer is KMP, and the standard library dutifully implements it. Measured on real
JVMs, that's the wrong call: KMP's per-element state machine defeats the very loop optimizations
that make scans fast, to guard against pathological inputs that collection code essentially never
sees.

FArray's slice search is a **candidate scan**: use the short-circuit engine's `indexOf` — the
fastest thing on this page — to find occurrences of the needle's *first element*, and verify the
rest of the needle only at those candidate positions. The common case does a raw unboxed scan for
one value and a handful of short verifications:

<BenchPair
  int="SearchSliceIntBenchmark"
  str="SearchSliceStrBenchmark"
  caption="The slice-search family, 3-element needle from the middle of the haystack, serialized measurement. On Int the candidate scan runs indexOfSlice at 2.2–2.4×, containsSlice at 2.2–2.4×, and lastIndexOfSlice at 2.4–2.8× over the best rival from 10 elements up — where the previous KMP-shaped implementation measured 0.76×, a loss. startsWith/endsWith are bounded compares at ~1.3× (and O(1)-decided at sizes 0–1, hence the 2.4–4× cells there). sameElements and segmentLength tie the field, as memory-bound compares should. The String twin shows how much survives when every compare is an equals call."
/>

The honest boundary: a pathological input (long needle of one repeated value over a haystack of the
same value) degrades toward O(n·m), which KMP's guarantee avoids. Collection workloads don't look
like that, and the 0.76× → 2.2× swing is what the guarantee was costing on everything else.

<Related
  links={[
    { to: "/operations/indexed", label: "Indexed access", desc: "The other read-only family, and the honest wrapper tax." },
    { to: "/fusion/terminals", label: "Fused search terminals", desc: "find/exists that stop a whole pipeline, not just a scan." },
    { to: "/operations/folds", label: "Folds & reduces", desc: "The scans that don't stop." },
  ]}
/>
