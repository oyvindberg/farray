---
title: "Fast without fusion"
sidebar_label: Without fusion
description: "Eager FArray, no .fuse anywhere, already beats List, Vector, IArray and the Chunks. Single ops tie a raw array; chaining is where the model compounds, shown Int beside String."
---

import Link from "@docusaurus/Link";
import BenchPair from "@site/src/components/BenchPair";
import ChainScore from "@site/src/components/ChainScore";
import Related from "@site/src/components/Related";

{/* This page is eager-only by construction. The fused/lazy pipelines belong with the fused
    comparison, not here: java.util.stream (a lazy fused pipeline) and FArray's own .fuse. Every
    chart drops them. (No chains benchmark has a farrayEager bar; the eager FArray method is just
    `farray`.) */}
export const fused = ["javastream", "farrayFused"];

<p className="lede">
  The introduction led with fusion, FArray's headline trick, which has{" "}
  <Link to="/fusion">its own section</Link>. This page is the plain library: no <code>.fuse</code>
  anywhere, just eager <code>FArray</code>, and it already beats the field. Single ops tie a raw
  array; chaining is where it pulls ahead, so that is what these benchmarks show, Int on the left and
  String on the right.
</p>

The rivals throughout: Scala's `List` (an immutable linked list, not `java.util.List`), `Vector`
(a persistent tree), `IArray` (an immutable view of a raw array), and the array-backed `Chunk`
types from the fs2 and ZIO streaming libraries.

First the totals. Every size of every chained-operation benchmark class in the suite, eager FArray
only, no `.fuse`:

<ChainScore />

Three effects compound down a chain:

<ul className="points">
  <li>
    <strong>Boxing is paid per stage.</strong> A three-stage pipeline over boxed elements boxes at
    every crossing; chaining multiplies the tax rather than amortizing it. FArray's stages hand
    each other primitive arrays.
  </li>
  <li>
    <strong>Structural stages cost nothing.</strong> A <code>take</code>, <code>drop</code> or{" "}
    <code>reverse</code> in the middle of a chain is an O(1) lazy node for FArray and a full copy for
    every array-backed rival, a copy of data whose only purpose is to be consumed by the next stage.
  </li>
  <li>
    <strong>Big methods slow the rivals down.</strong> The array-backed collections run every lambda
    you pass to <code>map</code>/<code>filter</code> through one shared, generic copy of that method.
    Pack several different lambdas into one method and the JIT can no longer keep that shared copy
    specialized, so it deoptimizes and slows down. FArray compiles each lambda into its own loop, so
    there is nothing shared to spoil. This is what turns a one-line benchmark tie into a loss in real
    code, shown in full <Link to="#the-method-size-effect-eight-distinct-maps">further down</Link>.
  </li>
</ul>

## The classic: map → filter → fold

Transform, select, aggregate: the shape of half the collection code ever written. One boxed
crossing per stage for the field; `int[]` end to end for FArray:

<BenchPair
  int="MapFilterFoldIntBenchmark"
  str="MapFilterFoldStrBenchmark"
  ignore={fused}
  caption="map → filter → foldLeft, Int beside String. The boxing collections pay per stage (5.6× over Vector, 12× over List at 100k); IArray and zio.Chunk pay only the intermediate allocations. int[] end to end for FArray, and eager FArray beats eager IArray at every size — its filter is branchless and vectorizes. The lazy single-pass rivals (java.util.stream, and FArray's own .fuse, which both fuse the three stages into one loop with no intermediates) are compared on the fusion benchmarks page; here the field is eager."
/>

## Structure in the middle: take → drop → map → fold

Slice stages are FArray's freebie: a lazy window over the source, no copy. A `map` then transforms
through the window, and the fold reads straight out of the result:

<BenchPair
  int="TakeDropMapFoldIntBenchmark"
  str="TakeDropMapFoldStrBenchmark"
  ignore={fused}
  caption="take(n-1) → drop(1) → map → foldLeft. FArray's take/drop are O(1) SliceNodes: the map runs over the window without either slice ever being copied, then the fold consumes its result. The array-backed rivals copy at each slice before they even reach the map, and the boxing collections box the map stage on top."
/>

## Structure at the end: filter → map → reverse

The mirror case: the structural stage last. FArray's `reverse` is a node pointing at the result;
everyone else allocates a third collection just to flip the second one:

<BenchPair
  int="FilterMapReverseIntBenchmark"
  str="FilterMapReverseStrBenchmark"
  ignore={fused}
  caption="filter → map → reverse. The first two stages are the usual per-element story; the reverse is where FArray stops doing work: an O(1) ReverseNode versus a full reversed copy. (fs2.Chunk has no reverse and sits out.)"
/>

## Expansion: flatMap → flatMap → map → fold

Chained expansion stresses what a library hands its next stage. Each FArray `flatMap` produces one
flat leaf, so the following stage reads a tight array rather than a fragment chain:

<BenchPair
  int="FlatMapChainIntBenchmark"
  str="FlatMapChainStrBenchmark"
  ignore={fused}
  caption="flatMap → flatMap → map → foldLeft with small inners. Every stage's output is a flat leaf, so the compounding is linear for FArray; the boxing collections pay both the per-element tax and the intermediate allocation, twice over."
/>

## The method-size effect: eight distinct maps

This chain exposes the shared-call-site problem: eight *different* lambdas through `map` in one
method, the profile any nontrivial method builds up.

💣 **A landmine hides in the primitive case.** Watch IArray on `Int` (left chart below): dead even
with FArray through 1k, then it drops to a *third* of FArray's speed at 10k and never recovers. The
same code, one input-size step, a 3× loss from nowhere.

<details>
<summary>Why it detonates, and only at scale</summary>

**What:** the same eight-map method, run at one size larger, loses 3× against the identical FArray
code, with no source change of any kind.

**How:** first, what it is *not*. The allocation profiler shows FArray and IArray allocating
byte-for-byte the same here, eight `int[]` per call (~32 bytes per element), with **no boxing on
either side** — an `Int => Int` lambda is specialized (`apply$mcII$sp`), so nothing boxes even when
the map isn't inlined. The gap is pure compute. IArray runs all eight lambdas through its one shared
`Array.map`; once that call site has seen three or more `Function1` (Scala's lambda interface)
classes it is megamorphic and the JIT can no longer inline it. Instead of eight tight, inlined
`int[]` loops, the elements then run through one generic, unspecialized loop with a virtual call per
element. On trivial `Int` work (`_ + 1`) that per-element overhead is the entire cost, so it shows as
a multiple; on `String`, where each element already does real allocating work, the same overhead is
a rounding error, which is why the field stays close there. It ties at 1k because small inputs stay
near the fixed per-call costs; the per-element penalty only takes over once there are enough elements
to feel it.

FArray has no shared site to poison: each lambda is its own monomorphic compiled loop, so eight maps
are eight tight `int[]` loops however many you write.

</details>

<BenchPair
  int="MapMegaIntBenchmark"
  str="MapMegaStrBenchmark"
  ignore={fused}
  caption="The full field, Int beside String. On Int (left) FArray and IArray run dead even through 1k (IArray's last stand, 0.97×), then FArray opens to ~3.4× ahead at 10k and holds there; List trails 11×, Vector 5.6×. On String (right) the same megamorphic call site exists, but the per-element string work is heavy enough to swamp its overhead, so nobody pulls away — which is the tell that the cost is per-element dispatch, not allocation."
/>

## A long interleaved structural chain

The last chain alternates `+:`, `:+`, `++` (prepend, append, concat) and
`reverse`, forced by one final `map`. For FArray that's a string of O(1) nodes and a single O(n)
traversal; for array-copying structures it's a full copy *per op*:

<BenchPair
  int="StructuralShowcaseIntBenchmark"
  str="StructuralShowcaseStrBenchmark"
  ignore={fused}
  caption="An interleaved prepend/append/concat/reverse chain, materialized once at the end. O(n) total for FArray versus O(n·chain-length) for the copiers: this is the thousands-× regime, identical on Int and String because structural ops never touch elements."
/>

---

There is one speed above this: handing the whole chain to the compiler.{" "}
<Link to="/fusion">Fusion</Link> takes these same pipelines and emits a single loop; the 14-stage
version runs ~15× past even FArray's own eager chains. The eager ops above are already the
strongest field; fusion is the ceiling.

<Related
  links={[
    { to: "/fusion/benchmarks", label: "The fused receipts", desc: "The same argument, one loop deeper." },
    { to: "/benchmarks/farray", label: "The scoreboard", desc: "Every op at every size, boiled to one number." },
    { to: "/benchmarks/farray", label: "The full suite", desc: "All charts, including every chain here." },
  ]}
/>
