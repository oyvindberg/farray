---
title: Fusion benchmarks
sidebar_label: The receipts
description: "The fused pipeline measured warm, cold, streaming, and on the chain that used to collapse, with every bar hoverable and every @Benchmark source one click away."
hide_table_of_contents: true
---

import Link from "@docusaurus/Link";
import Snippet from "@site/src/components/Snippet";
import SideBySide from "@site/src/components/SideBySide";
import BenchChart from "@site/src/components/BenchChart";
import BenchPair from "@site/src/components/BenchPair";
import Related from "@site/src/components/Related";

<p className="page__eyebrow">the receipts</p>

<p className="lede">
  Everything this section claimed is measured here. Every bar is a JMH sweep you can hover; every card
  reveals the exact <code>@Benchmark</code> methods behind it, so you can check that each rival runs
  its best idiom rather than a strawman.
</p>

## The headline: fourteen stages, warm

The pipeline from the introduction: `flatMap`, `filter`, `map`, `zip`, `zipWithIndex`, `foldLeft`
(a seeded reduce), twice over. Every collection runs the identical code; the FArray entry differs
by the one word:

<SideBySide
  snippet="fuse-pipeline"
  cls="LongMixedPipelineIntBenchmark"
  title="14-stage pipeline"
  caption={
    <>
      Swept from empty to 100k elements. At 100k: ~36× the quickest rival, ~83× List, and ~15× the
      same pipeline run eagerly on FArray itself (the gap between compiled-once ops and a compiled
      pipeline). Hover any column for exact ratios; the fused
      variant's allocation profile measures at inputs-only (4·N + 40 bytes per invocation, the
      benchmark's own zip source), with the pipeline contributing zero.
    </>
  }
/>

## Against java.util.stream: the same pipeline, both fused

`java.util.stream` is the JVM's own fused pipeline, which makes it the fair rival for `.fuse` and
the wrong one for the eager chains on the <Link to="/chains">chains page</Link> (comparing a lazy
pipeline against eager materialization would be a strawman, so it sits this one out there). Here is
the classic map → filter → foldLeft with both sides collapsed to a single pass:

<BenchPair
  int="MapFilterFoldIntBenchmark"
  str="MapFilterFoldStrBenchmark"
  caption="map → filter → foldLeft. Eager FArray and every eager collection materialize two intermediate arrays across three passes; IntStream and FArray's .fuse both run it as one pass with none. On Int, IntStream leads the EAGER FArray chain ~1.5×, and .fuse retakes the lead at 1.1–1.2×. On String the per-element String allocation dwarfs the intermediate-array traffic, so eager already nearly ties and fusion changes little. The eager FArray bar is the same measurement shown on the chains page; the fused bar is the point of this chart."
/>

## Cold: no warmup lap

Because the specialization is in the bytecode, there is no lap where a boxing collection is briefly
ahead:

<BenchChart
  cls="ColdPipelineIntBenchmark"
  caption="The same 14-stage pipeline measured COLD: SingleShotTime, zero warmup, one uncompiled invocation at 100k elements (invocations/sec). Fused FArray is ~3.8× past List and IArray, ~2.7× past the quickest rival, before the JIT compiles anything. Warmup only changes the size of the lead."
/>

## Streaming: constant memory without the streaming tax

The chunk-granular <Link to="/fusion/sources">source model</Link>, against the JVM's usual ways of
processing data you don't want in memory:

<BenchChart
  cls="StreamPipelineBenchmark"
  caption="filter → map → sum over a lazily GENERATED sequence (vs Iterator, LazyList (Scala's lazy linked list), fs2.Stream) and over an InputStream of lines (vs scala.io.Source and a hand-rolled BufferedReader loop). Chunks amortize the pull; the per-element cost is the fused loop."
/>

## The chain that used to collapse

The case for compiling the pipeline instead of relying on the JIT, preserved: `flatMap → filter → take`
once ran at 0.055× of IArray when a trailing structural call silently disabled escape analysis on a
spliced loop (<Link to="/operations/flatmap">the full story</Link>). Compiling the chain to one loop
removes any dependence on the caller's method size or call-site history, so that failure mode cannot
recur. Eager FArray already runs the shape well ahead of the field — up to 22× past List — on the{" "}
<Link to="/chains#flatmap--filter--take">chains page</Link>; fusion is the same shape
with the intermediates gone.

## The other JIT: Temurin/C2

Everything above was measured on GraalVM CE, and fusion splices whole pipelines into single
methods, exactly the shape where JIT inliners differ most. So the headline claims were re-run on
the JVM most production fleets deploy: Temurin 25 (HotSpot C2), same machine, one fork per trial.

| Temurin 25, C2 | eager FArray | fused FArray | best rival |
| --- | --- | --- | --- |
| 14-stage pipeline, 100k | 2.97× rival | **37.7× rival** (12.7× over eager) | zio.Chunk |
| 14-stage pipeline, 1k | 2.90× rival | **22.6× rival** (7.8× over eager) | zio.Chunk |
| map→filter→fold Int, 100k | 0.49× rival | **1.09× rival** (2.2× over eager) | IntStream |

Two findings, both worth stating plainly. The 14-stage headline holds on C2: 37.7× against 36× on
Graal; the speed genuinely lives in the emitted bytecode. And the short chain is where C2 tells a
different story: HotSpot compiles `IntStream`'s three-stage sink chain extremely well in a small
method, well enough to run *ahead of eager FArray* (which pays two intermediate arrays) and within
9% of fused. Runtime fusion is a real competitor on its best JIT at its best method size; the gap
that never closes is the one the megamorphic and method-growth pages measure, and the 14-stage
column above.

## JSON projection queries

The byte-source numbers live with their scanners and rivals on{" "}
<Link to="/fusion/json">the JSON integration page</Link>. The headline: 1.6× past jsoniter-scala's
hand-written reader loop at ~1 B/op, 4–9× past the AST parsers, and 2.5× on selective queries where
records are abandoned mid-scan.

---

Method: the checked-in `docs/bench-results.json`, produced by `scripts/bench-run.sh` (JMH, GraalVM
CE 25, Apple Silicon; farray-only patch mode re-measures our methods against cached rival runs; see
the repo's benchmarking notes). Cold numbers are SingleShotTime; allocation numbers are `-prof gc`.
The Temurin/C2 table above is a separate forked run (`BENCH_JAVA` override in the same script).

<Related
  links={[
    { to: "/benchmarks/farray", label: "The full FArray suite", desc: "Every op at every size, wins and losses alike." },
    { to: "/fusion", label: "Back to first principles", desc: "Why these numbers don't depend on the call site." },
  ]}
/>
