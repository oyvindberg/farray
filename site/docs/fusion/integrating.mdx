---
title: Integrating with fusion
sidebar_label: Integrating
description: "The library-author page: what a fusion integration provides (shape, lowering, source, decoder), what the engine keeps, and the contract at every seam."
---

import Link from "@docusaurus/Link";
import Snippet from "@site/src/components/Snippet";
import Related from "@site/src/components/Related";

<p className="page__eyebrow">for library authors</p>

<p className="lede">
  Everything before this page is about using pipelines; this page is about giving the engine a new
  thing to run them over. An integration provides four pieces, in ascending order of strangeness: a{" "}
  <strong>source</strong> that frames records out of bytes (ordinary I/O code), a{" "}
  <strong>shape</strong> that names your format in the type, a <strong>lowering</strong> that says
  which terminals your shape supports, and a <strong>decoder</strong> that reads one record's
  fields at macro time. The engine keeps everything else: live-set analysis, dead-field skipping,
  predicate placement, memoization, the loop. We take the pieces in that order, easiest first.
</p>

## The familiar half: chunks and records

This part is ordinary code, no compiler involvement, but it rests on a distinction worth being
precise about, because the whole memory story hangs on it. A byte source deals in two different
units:

- **A chunk is a unit of memory.** The source holds exactly one block of bytes at a time: `buf`,
  advanced by `nextChunk()`. A file source reads block after block; an in-memory source answers
  `nextChunk()` with `true` exactly once and its whole array. This is the constant-memory
  guarantee: nothing upstream ever demands two blocks at once.
- **A record is a unit of meaning.** Within the current chunk, `nextRecord()` frames the next
  complete record as a range, `recordStart` until `recordEnd`, no copy and no allocation; the
  generated scanner reads fields straight out of `buf` between those bounds. For NDJSON a record
  is a line; for a length-prefixed format it would be a length header's worth of bytes.

The engine's runtime loop is exactly the nesting you'd guess, with `close()` in a `finally` that
fires on exhaustion, short-circuit and exception alike:

```scala
while (source.nextChunk())            // memory: one block resident
  while (source.nextRecord())         // meaning: one complete frame
    scan(source.buf, source.recordStart, source.recordEnd)
```

The two units don't line up: records do not respect block boundaries. That misalignment is modeled
as a small ADT, and making "unfinished record" a first-class value instead of an offset somebody
has to remember is what keeps the boundary logic in exactly one place:

<Snippet name="framed-adt" hideFull />

The place is `FramedByteSource`, which farray provides: it owns the working buffer, the block
reads, and the carry/stitch for a record that straddles two reads (compact the partial tail to the
front, read the next block behind it, re-frame). Memory is therefore one block plus the largest
record, and the engine never sees the seam. What's left for a format is one function from a byte
window to a verdict; here is the NDJSON framer, in its entirety:

<Snippet name="ndjson-frame" hideFull />

A length-prefixed binary format would read a header instead of scanning for a newline; the four
verdicts and everything downstream of them are unchanged. The in-memory source skips all of this
(one chunk, no straddling) and is a screenful, plus `def stream` to enter the pipeline:

<Snippet name="ndjson-source" hideFull />


## Where terminals come from

None of the terminal methods is defined on the pipeline class. `Fuse[A, S]` carries only the stage
markers; `S` is the pipeline's source shape (<Link to="/fusion/sources">the sources page</Link>),
and the terminals are inline extension methods (methods bolted onto a type from outside it,
resolved statically at compile time) provided by the shape's `FuseLowering[S]` given (Scala's
keyword for publishing a typeclass instance the compiler locates by type), published in the shape's
companion object, so it rides the implicit scope of every `Fuse[A, S]` with no imports at the use
site.

Three consequences:

<ul className="points">
  <li>
    <strong>Scope lives in the types.</strong> A shape mixes in the terminal families it supports:
    the capability traits <code>AggTerminals</code>, <code>SearchTerminals</code>,{" "}
    <code>GroupTerminals</code>, <code>PlanTerminals</code>, <code>MaterializeTerminals</code>{" "}
    (bundled as <code>StandardTerminals</code>). A terminal a shape doesn't mix in is <em>not a
    member</em> of its pipelines and doesn't compile; there is no runtime error and no slow fallback.
  </li>
  <li>
    <strong>The syntax is written once.</strong> Those traits contain every terminal method, each a
    one-liner that reifies the call into a typed <code>Terminal[A, R]</code> marker
    (<code>Terminal.find(p): Terminal[A, Option[A]]</code>) and hands it to the one abstract hook a
    lowering implements.
  </li>
  <li>
    <strong>The hook is the whole integration surface.</strong> A lowering module implements a
    single <code>inline def lower(self, t)</code> whose macro forwards pipeline + terminal to the
    engine, together with the module's decoder, as an ordinary method argument at compile time. No
    registry, no reflection, no service files.
  </li>
</ul>

The native shape (`Chunks`, covering in-memory arrays and chunk streams) mixes in everything. The
NDJSON shape below takes `StandardTerminals` and skips `MaterializeTerminals`, which is why
`Json.ndjson[T](bytes).stream.run` is a compile error while `.sum` is a byte-level scanner. The
difference between two shapes is spelled out in their mixin lists.

## The whole integration, one file

The NDJSON plug-in, verbatim: the shape, the given, and the hook.

<Snippet name="json-integration" hideFull />

`Ndjson` is the source shape, and the given in its companion puts `JsonLowering`'s terminals on
every `Fuse[A, Ndjson]`. `StandardTerminals[Ndjson]` is where the terminal *methods* come from,
and every one of them funnels into the single abstract `lower` you see in the file.

The last four lines are the hook, and they exist to solve one problem: the engine and your format
have never met. `FuseMacro` lives in farray and can compile any pipeline into a loop, but it has no
idea how to read your bytes; your module owns the decoder, but has no loop compiler. The hook is
the meeting point, and it has to happen inside the compiler, because that is where the loop gets
generated.

```scala
inline def lower[A, R](inline self: Fuse[A, Ndjson], inline t: Terminal[A, R]): R =
  ${ JsonLowering.impl[A, R]('self, 't) }

def impl[A: Type, R: Type](self: Expr[Fuse[A, Ndjson]], t: Expr[Terminal[A, R]])(using Quotes): Expr[R] =
  FuseMacro.lower(self, t, JsonDecode)
```

What actually happens when a user writes `pipeline.sum`: the terminal method reifies the call and
lands here, in `lower`, during compilation. Three things go across to the engine: the pipeline as
the user wrote it, the terminal that ended it, and `JsonDecode`, your decoder. The engine builds
the scanner loop out of them and returns it, and that loop is compiled into the user's program in
place of the `.sum` call. By runtime, these four lines have no existence; only the loop they
brokered remains.

Why it takes two declarations and this exact punctuation: that is Scala 3's required ritual for
crossing from ordinary code into compiler-executed code and back. It is not a place where your
integration does anything. Copy the four lines verbatim; the only token that is yours is the
decoder's name.

## The decoder contract

The decoder is the format-specific work, and its contract with the engine is one conversation, at
macro-expansion time. The engine calls the decoder once per pipeline with a request carrying: the
record's case-class type, the runtime byte source, the pipeline's `map` and `filter` lambdas plus
the terminal's element readers (the decoder walks these to compute the *live set*, which fields
anything downstream actually reads), a short-circuit flag for `take`/`find`, and a `continue`
callback the engine owns. The decoder answers with one column per record field, in declaration
order: each column is a read thunk that emits the byte-level code for that field, or a skip plus
default constant if the field is dead.

That's all it answers; the engine takes the columns and does the rest: memoizing String reads so a
field decodes at most once, placing predicates for the mid-record early-out, assembling whatever
the terminal needs, and emitting the loop. That split of labor is why the decoder stays small, and
why a CSV or length-prefixed-binary decoder would be the same shape: implement the framing,
implement the per-field reads, publish a given.

<Related
  links={[
    { to: "/fusion/json", label: "The worked example", desc: "The NDJSON integration running real queries, with scanners and rivals." },
    { to: "/fusion/terminals", label: "The terminals", desc: "What the capability traits actually grant." },
    { to: "/fusion/sources", label: "Sources & shapes", desc: "Where S comes from and what it promises." },
  ]}
/>
